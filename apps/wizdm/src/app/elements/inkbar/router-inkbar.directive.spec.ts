import { RouterInkbarDirective } from './router-inkbar.directive';

describe('RouterInkbarDirective', () => {
  it('should create an instance', () => {
    const directive = new RouterInkbarDirective();
    expect(directive).toBeTruthy();
  });
});
