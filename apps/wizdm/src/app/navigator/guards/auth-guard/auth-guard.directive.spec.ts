import { AuthGuardDirective } from './auth-guard.directive';
/*
describe('AuthGuardDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthGuardDirective();
    expect(directive).toBeTruthy();
  });
});
*/