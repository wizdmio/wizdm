import { ViewportDirective } from './viewport.directive';
/*
describe('ViewportDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewportDirective();
    expect(directive).toBeTruthy();
  });
});

*/
