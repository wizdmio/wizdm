import { ScrollHide } from './scrollbar-utils.directive';

describe('ScrollHide', () => {
  it('should create an instance', () => {
    const directive = new ScrollHide();
    expect(directive).toBeTruthy();
  });
});
