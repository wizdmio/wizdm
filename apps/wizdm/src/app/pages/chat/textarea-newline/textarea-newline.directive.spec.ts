import { TextareaNewlineDirective } from './textarea-newline.directive';

describe('TextareaNewlineDirective', () => {
  it('should create an instance', () => {
    const directive = new TextareaNewlineDirective();
    expect(directive).toBeTruthy();
  });
});
