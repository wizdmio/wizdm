import { TypeinAdapterDirective } from './typein-adapter.directive';

describe('TypeinAdapterDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeinAdapterDirective();
    expect(directive).toBeTruthy();
  });
});
