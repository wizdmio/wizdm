import { OlderThanPipe } from './older-than.pipe';

describe('OlderThanPipe', () => {
  it('create an instance', () => {
    const pipe = new OlderThanPipe();
    expect(pipe).toBeTruthy();
  });
});
