import { ButtonMediaChangerDirective } from './button-media-changer.directive';

describe('ButtonMediaChangerDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonMediaChangerDirective();
    expect(directive).toBeTruthy();
  });
});
