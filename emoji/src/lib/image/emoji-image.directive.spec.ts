import { EmojiImage } from './emoji-image.directive';
/*
describe('EmojiImage', () => {
  it('should create an instance', () => {
    const directive = new EmojiImage();
    expect(directive).toBeTruthy();
  });
});
*/