import { EmojiControl } from './emoji-control.directive';
/*
describe('EmojiControl', () => {
  it('should create an instance', () => {
    const directive = new EmojiControl();
    expect(directive).toBeTruthy();
  });
});*/
