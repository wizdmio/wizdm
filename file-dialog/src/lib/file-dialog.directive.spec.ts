import { FileDialogDirective } from './file-dialog.directive';

describe('FileDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new FileDialogDirective();
    expect(directive).toBeTruthy();
  });
});
