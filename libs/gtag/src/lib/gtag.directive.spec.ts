import { GtagDirective } from './gtag.directive';

describe('GtagDirective', () => {
  it('should create an instance', () => {
    const directive = new GtagDirective();
    expect(directive).toBeTruthy();
  });
});
