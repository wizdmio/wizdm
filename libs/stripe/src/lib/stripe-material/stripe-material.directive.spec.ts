import { StripeMaterialDirective } from './stripe-material.directive';

describe('StripeMaterialDirective', () => {
  it('should create an instance', () => {
    const directive = new StripeMaterialDirective();
    expect(directive).toBeTruthy();
  });
});
