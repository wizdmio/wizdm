import { TeleportDirective } from './teleport.directive';
/*
describe('TeleportDirective', () => {
  it('should create an instance', () => {
    const directive = new TeleportDirective();
    expect(directive).toBeTruthy();
  });
});
*/