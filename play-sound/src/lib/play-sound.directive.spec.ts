import { PlaySoundDirective } from './play-sound.directive';

describe('PlaySoundDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaySoundDirective();
    expect(directive).toBeTruthy();
  });
});
