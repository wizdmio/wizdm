import { RedirectDirective } from './redirect.directive';
/*
describe('RedirectDirective', () => {
  it('should create an instance', () => {
    const directive = new RedirectDirective();
    expect(directive).toBeTruthy();
  });
});
*/