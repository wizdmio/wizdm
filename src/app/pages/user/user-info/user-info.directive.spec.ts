import { UserInfoDirective } from './user-info.directive';

describe('UserInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new UserInfoDirective();
    expect(directive).toBeTruthy();
  });
});
