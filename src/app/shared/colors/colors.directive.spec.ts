import { ColorsDirective } from './colors.directive';

describe('ColorsDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorsDirective();
    expect(directive).toBeTruthy();
  });
});
