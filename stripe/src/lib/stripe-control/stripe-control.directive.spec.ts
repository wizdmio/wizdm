import { StripeControlDirective } from './stripe-control.directive';

describe('StripeControlDirective', () => {
  it('should create an instance', () => {
    const directive = new StripeControlDirective();
    expect(directive).toBeTruthy();
  });
});
