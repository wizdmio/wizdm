import { StripeElements } from './stripe-elements.directive';
/*
describe('StripeElementsDirective', () => {
  it('should create an instance', () => {
    const directive = new StripeElementsDirective();
    expect(directive).toBeTruthy();
  });
});
*/