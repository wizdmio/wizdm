import { PortalDirective } from './portal.directive';

describe('PortalDirective', () => {
  it('should create an instance', () => {
    const directive = new PortalDirective();
    expect(directive).toBeTruthy();
  });
});
