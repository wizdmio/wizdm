import { FabDirective } from './fab.directive';

describe('FabDirective', () => {
  it('should create an instance', () => {
    const directive = new FabDirective();
    expect(directive).toBeTruthy();
  });
});
