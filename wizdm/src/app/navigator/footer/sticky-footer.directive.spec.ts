import { StickyFooterDirective } from './footer.directive';

describe('StickyFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyFooterDirective();
    expect(directive).toBeTruthy();
  });
});
