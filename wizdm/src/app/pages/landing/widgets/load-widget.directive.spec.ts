import { LoadWidgetDirective } from './load-widget.directive';

describe('LoadWidgetDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadWidgetDirective();
    expect(directive).toBeTruthy();
  });
});
