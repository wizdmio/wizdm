import { CanLeaveDirective } from './can-leave.directive';
/*
describe('CanLeaveDirective', () => {
  it('should create an instance', () => {
    const directive = new CanLeaveDirective();
    expect(directive).toBeTruthy();
  });
});
*/
