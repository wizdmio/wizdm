import { SizeLockDirective } from './size-lock.directive';

describe('SizeLockDirective', () => {
  it('should create an instance', () => {
    const directive = new SizeLockDirective();
    expect(directive).toBeTruthy();
  });
});
