import { TextareaNewline } from './textarea-newline.directive';

describe('TextareaNewline', () => {
  it('should create an instance', () => {
    const directive = new TextareaNewline();
    expect(directive).toBeTruthy();
  });
});
